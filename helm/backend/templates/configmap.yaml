apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name.deployment }}-configmap
  labels:
    {{- include "backend.labels" . | nindent 4 }}
data:
  APP_VERSION: {{ .Values.appVersion | quote }}